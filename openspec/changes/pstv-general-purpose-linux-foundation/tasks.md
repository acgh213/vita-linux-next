# Tasks

> **For Hermes:** After a separate implementation request, use the subagent-driven-development skill task-by-task, with spec compliance and independent review. This document does not authorize execution, branch changes, commits, PRs or hardware operations.

**Goal:** Turn the recorded PSTV USB Debian experiment into the reproducible and explicitly gated foundation defined in `proposal.md`.

**Architecture:** Extend the existing Buildroot early-boot and `debian/` integration, retaining the pinned kernel/loader and independent rescue. Host fixtures, image inspection and real PSTV acceptance are distinct deliverables; the hardware phase has a hard human-authorization boundary.

**Tech stack:** Vita Linux 6.12, Buildroot rescue, Debian bookworm armhf/sysvinit, ext4 image on USB exFAT, POSIX shell/Python fixtures, debootstrap/QEMU host assembly, apt/dpkg native acceptance.

## 1. Execution conventions and bounded research

All repository paths below are relative to `/home/cassie/projects/vita-linux-next` unless labelled **research**. All tasks are **unchecked**: none was performed by writing this plan. New paths/interfaces are explicitly identified; commands using them are **planned verification commands**, not commands already available. Existing source is inspected at `91378d570a06aa354482f72b460eb4e45f28b3f7` and must be rechecked before implementation without resetting it.

Use `E=lab/pstv-foundation/<run-id>` for future public-safe evidence and `W=/home/cassie/build/pstv-foundation/<run-id>` for future private/build scratch. The executor must choose a unique run ID, verify the actual scratch mount/space, and record the expanded paths. These are runbook variables, not directories created in this planning session. No private material is written to E.

For every host implementation task: add its stated regression first, capture the expected RED failure, implement the smallest fix, capture GREEN and relevant regressions. Do not copy expected output into evidence. Each task's named artifact and completion condition must be met before checking it off. A failed experiment can complete an investigation task with a negative disposition, but cannot complete dependent positive implementation/hardware acceptance tasks. The pinned kernel gitlink already contains the corrected private timer clock (design E8): do not re-apply, re-derive or bundle a timer/ref-clock DT change, and do not treat the inherited 144 MHz value as current.

- [ ] 1.1 **[Research / read-only] Reconcile the starting evidence ledger.** Prerequisite: new implementation request. Create `docs/evidence/pstv-foundation.md` with report/raw-output distinctions, chronological supersession, source pins and missing-gate matrix. It must also reconcile the four recorded conflicts instead of summarizing over them: the `root-config.log` stop-graph FAIL versus the `RESULTS.md` shutdown-order claim, the 22,872/22,878 file-count difference, the `transfer-health.txt` exFAT not-properly-unmounted warnings versus the reported zero `unclean-shutdown` records, and the stale RAM-only rootfs wording in the research README. Verify with `git status --short --branch`, `git rev-parse HEAD`, `git ls-tree HEAD linux_vita buildroot vita-baremetal-linux-loader`, and a citation/path review against research `lab/usb-debian-2026-09-13/{RESULTS.md,MANIFEST.md,device-verify.log,boot-debian.log}`. Produce `E/orientation.md`. Complete when every baseline claim identifies its venue/artifact and no historical record is relabelled as current device state; research stays read-only.

- [ ] 1.2 **[Research / package inputs] Resolve the signed bookworm input set and real upgrade fixture.** Prerequisite: 1.1. Inspect `debian/packages.txt`; create `debian/inputs.lock.json` plus `debian/inputs-lock.schema.json`, recording suite/armhf, authenticated archive/keyring/index identities, exact resolved package versions/hashes and host bootstrap/toolchain inputs. Record current suite support/availability with authoritative source URLs/date, and select one authenticated package version pair for install then actual upgrade. Verification: independent signature/hash validation and version comparison recorded in `E/package-inputs.md`; no target contact. Complete when inputs are fully resolvable and the upgrade pair differs; unavailable inputs produce a blocked decision, not an implicit trixie switch or unauthenticated fallback.

- [ ] 1.3 **[Research / lifecycle source audit] Pin the real SysV stop graph and terminal ownership.** Prerequisites: 1.1–1.2. Inspect source for the locked `sysv-rc`, `initscripts` and init packages, `debian/insserv-overrides/`, `debian/overlay/etc/init.d/`, and rescue storage services. Write `debian/LIFECYCLE.md`: actual caller chain, rc0/rc6 behavior after a nonzero script, `/run` lifetime, final backing-store action, guarded terminal-hook/diversion locations and restoration procedure. Verification: cite exact locked package source paths and trace one success and each failure through the terminal action in `E/lifecycle-source-audit.md`. Complete when the D5 cooperative interlock has explicit owners and fault signals; if a proposed mechanism cannot meet the spec, stop for a design revision rather than implementing a success-shaped placeholder.

## 2. Host contracts and isolated assembly entrypoints

- [ ] 2.1 **[Host implementation] Define enrollment and artifact contracts with refusal fixtures.** Prerequisites: 1.1–1.2. Create `debian/root-contract.schema.json`, `debian/artifact-manifest.schema.json`, `tests/fixtures/foundation/` and `tests/test-foundation-contracts.py`. Cover wrong model/transport/architecture, missing pins, unsafe image paths, duplicate identities, partial manifests and distinction between transfer hash and mutable instance identity. Verification: `python3 tests/test-foundation-contracts.py` RED then GREEN. Produce `E/contracts-tests.txt`; complete when invalid inputs fail with bounded reasons and a valid synthetic non-secret pair is accepted without device access.

- [ ] 2.2 **[Host implementation] Add non-mutating preflight.** Prerequisites: 2.1. Create `debian/build-rootfs.sh` preflight interface and `tests/test-debian-recipe.py`; document required host commands and writable-space budget in `debian/README.md`. Planned invocation: `bash debian/build-rootfs.sh --preflight --lock debian/inputs.lock.json --output "$W/build-a"`. Refuse raw devices, symlink escape, existing output, missing tools, wrong architecture and insufficient space. Verification: `python3 tests/test-debian-recipe.py` RED/GREEN with mounts, network and package-install commands stubbed to fail if called by preflight. Produce `E/preflight-tests.txt`; complete when preflight has no installation, mount or deployment side effects.

- [ ] 2.3 **[Host implementation] Implement locked foreign bootstrap and second stage.** Prerequisites: 1.2, 2.2. Extend `debian/build-rootfs.sh` and its tests using explicit foreign debootstrap, QEMU second stage and service-start suppression; consume the lock rather than the ambient mirror's latest packages. Add failure-cleanup tests for bootstrap/signature/download interruptions and bind-mount ownership, avoiding recursive copies of mounted proc/sys/dev. Verification: `python3 tests/test-debian-recipe.py` RED/GREEN. Produce `E/recipe-tests.txt`; complete when command/cleanup fixtures demonstrate no silent input substitution, existing-output overwrite or leaked owned mount. Real assembly is task 5.1, not this fixture result.

- [ ] 2.4 **[Host implementation] Separate private enrollment from public root assembly.** Prerequisites: 2.1, 2.3. Create `debian/provision-root.sh`, `tests/test-debian-provision.py` and a non-secret enrollment example in `debian/README.md`. Use `debian/overlay/etc/{network/interfaces,ssh/sshd_config.d/10-vita.conf,fstab}`; replace label-only root binding with the enrolled identity. Preserve private rescue access, provision persistent normal-root host identity, and keep firmware/credentials out of public outputs/logs. Planned interface: `bash debian/provision-root.sh --root "$W/private-root" --enrollment "$W/enrollment.json" --secrets-dir "$W/secrets"`; only future explicitly prepared private copies are allowed. Verification: `python3 tests/test-debian-provision.py` RED/GREEN, testing owner/modes, missing credentials, effective policy inputs, duplicate identity and public secret scan. Produce content-free `E/provision-tests.txt`; complete when accidental secret-bearing publication fails and missing access fails closed rather than associating with an arbitrary open AP.

## 3. Early root selection and recovery hardening

- [ ] 3.1 **[Host implementation] Require explicit foundation USB enrollment.** Prerequisite: 2.1. Modify `buildroot-vita/board/vita/overlay/init` and `tests/test-init-usb-root.sh`; add identity helpers only under `buildroot-vita/board/vita/overlay/usr/share/vita-boot/` if needed. Cover node renumbering, sysfs transport mismatch, wrong UUID/type, cloned candidates, absent USB with valid SD, malformed enrollment and bounded timeout. Verification: `bash tests/test-init-usb-root.sh` RED/GREEN. Produce `E/root-discovery-tests.txt`; complete when foundation mode never selects internal MMC/unapproved SD, the legacy compatibility profile and its existing SD-fallback test contract remain unchanged and outside acceptance, and every negative path reports a bounded reason.

- [ ] 3.2 **[Host implementation] Make admission read-only-first and resource-owned.** Prerequisite: 3.1. Modify the same `/init` and add `tests/test-root-admission.py`. Require a checker, replace automatic `e2fsck -p` with non-mutating refusal semantics, validate enrolled root identity/ABI/init, allocate a free loop, track mount ownership and verify unwinding after each failed move. Cover occupied loops, nonzero checker statuses, absent tools, dirty image, failed unmount and unchanged state after nominal success. Verification: `python3 tests/test-root-admission.py` and `bash tests/test-init-usb-root.sh` RED/GREEN. Produce `E/root-admission-tests.txt`; complete when fixtures prove no unintended detach, repair, write admission or exec on rejected candidates.

- [ ] 3.3 **[Host implementation] Isolate foundation rescue from legacy storage workers.** Prerequisites: 3.1–3.2. Modify `buildroot-vita/board/vita/overlay/etc/init.d/S06toolkit`, `buildroot-vita/board/vita/post_build.sh` and any competing auto-mounter explicitly identified in 1.3; add `tests/test-foundation-rescue.py`. Foundation profile suppresses automatic Workbench/USB mounts and retains local/independent network rescue; ordinary legacy profile remains separate. Verification: `python3 tests/test-foundation-rescue.py` RED/GREEN and `bash tests/test-post-build.sh`. Produce `E/rescue-ownership-tests.txt`; complete when rescue with absent/bad USB never launches a second storage owner or writes unrelated media, and emitted reasons remain visible.

- [ ] 3.4 **[Host implementation] Version and correlate boot-attempt state.** Prerequisites: 2.1, 3.2. Modify `buildroot-vita/board/vita/overlay/usr/share/vita-boot/select-root` and `tests/test-select-root-durability.py`. Add candidate/attempt correlation, three-consecutive-failure accounting, corrupt/partial/unknown-version/legacy state refusal, clock-jump invariance, empty/unreadable override distinction, CRLF, one-shot normal and evidence-preserving reset. Planned state format documented in `debian/BOOT-STATE.md`. Verification: `python3 tests/test-select-root-durability.py` RED/GREEN as an unprivileged user with deterministic injected write/sync failures, not permission-bit tests that accidentally pass under root. Produce `E/selector-tests.txt`; complete when every unsafe state selects rescue and intervening valid confirmation resets only the correct streak.

- [ ] 3.5 **[Host implementation] Make usable confirmation match a durable attempt.** Prerequisite: 3.4. Modify `debian/overlay/etc/init.d/vita-boot-confirm` and `tests/test-debian-lifecycle.py`; share the documented state protocol without mixing Debian and Buildroot libraries. Inject each missing readiness condition, mismatched identity, append failure, final-sync failure and stale-success record. Verification: `python3 tests/test-debian-lifecycle.py` RED/GREEN plus the selector test for next-boot behavior. Produce `E/confirmation-tests.txt`; complete when no failed/partial confirmation clears the current streak and effective readiness remains distinct from actual authenticated-access acceptance.

## 4. Graceful lifecycle and image inspectors

- [ ] 4.1 **[Host implementation] Make owned cleanup failures observable.** Prerequisites: 1.3, 3.5. Modify `debian/overlay/etc/init.d/{vita-loopdetach,vita-card-mounts}` and `tests/test-debian-lifecycle.py`. Keep optional attachments disabled in the foundation profile, but safely release any owned resources; propagate busy unmount, command enumeration failure and false-success detach. Never detach `/` or kill unrelated mount users. Verification: `python3 tests/test-debian-lifecycle.py` RED/GREEN, including occupied/unrelated loops and `/mnt/vita-card-old` prefix fixtures. Produce `E/cleanup-tests.txt`; complete when observed state, not pipeline exit masking, determines cleanup success.

- [ ] 4.2 **[Host implementation] Add conservative pending/synced shutdown state.** Prerequisites: 3.4, 4.1. Modify `debian/overlay/etc/init.d/vita-root-sync`, add `debian/overlay/etc/init.d/vita-shutdown-prepare`, update `debian/BOOT-STATE.md`, and extend selector/lifecycle fixtures. Implement the versioned `pending` -> `backing-flushed` -> `terminal-requested` transitions on the backing medium with explicit next-boot consumption/clear rules, and represent an unwritable or unsynced required record as terminal action blocked with an unknown next-boot outcome rather than as a durable rescue decision; `/run`-only state must not stand in for the durable record. Persist pending before stopping writers; verify root-ro/backing-rw; flush payload then completion evidence; retain conservative next-boot state when the required durable result cannot be established. Verification: both `python3 tests/test-debian-lifecycle.py` and `python3 tests/test-select-root-durability.py` RED/GREEN for every failure boundary. Produce `E/shutdown-state-tests.txt`; complete only when evidence distinguishes backing-flushed from unmounted and no missing failure append becomes a clean verdict.

- [ ] 4.3 **[Host implementation] Install package-safe terminal interlocks.** Prerequisites: 1.3, 4.1–4.2. Create `debian/lifecycle/install-hooks.sh`, `debian/lifecycle/terminal-guard`, `tests/test-terminal-guard.py`; modify `debian/insserv-overrides/{sendsigs,umountroot,umountfs}` as required by the audited caller graph. Implement the single interception point as diverted `/sbin/halt` and `/sbin/reboot` plus only the script bodies the caller graph requires, with separate rc0 and rc6 preconditions and one aggregate shutdown-result producer that ends `vita-card-mounts`' success-after-logged-failure behavior. Record exact stock paths/diversions in `debian/LIFECYCLE.md`. Stage default-deny runtime state in `/run`; real halt/reboot is called only after all owned stages and final backing handling pass. Test install/reinstall/removal and restoration of original hooks. Verification: `python3 tests/test-terminal-guard.py` RED/GREEN with real terminal operations stubbed, including earlier nonzero exits that SysV would otherwise ignore. Produce `E/terminal-guard-tests.txt`; complete when no injected failure reaches the terminal fixture and package reconfiguration cannot bypass the guard.

- [ ] 4.4 **[Host implementation] Verify the generated service graphs, not header strings alone.** Prerequisites: 4.3. Create `debian/verify-lifecycle.sh`, `tests/test-lifecycle-graph.py`; extend the builder to register all foundation services explicitly with insserv and exclude optional auto-mount services. Planned image interface: `bash debian/verify-lifecycle.sh --root "$W/build-a/rootfs"`. Verification: `python3 tests/test-lifecycle-graph.py` RED/GREEN using positive graphs and missing-service/cycle/reversed-order rc0/rc6 fixtures. Produce `E/graph-tests.txt`; complete when start/stop edges, late confirmation and terminal guards are independently checked and graph errors block image acceptance.

- [ ] 4.5 **[Host implementation] Build image and paired-manifest inspection.** Prerequisites: 2.4, 3.3, 4.4. Create `debian/inspect-rootfs.py`, `tests/test-debian-image.py`, `tests/test-foundation-artifacts.py`; extend `tools/vita-manifest.sh` and `tools/lib/vita-manifest-render.py` to reference the root/lock/enrollment classification, reusing `tools/lib/vita-extract-initramfs.py`. Cover missing rescue checker, wrong init/ABI, absent firmware/network prerequisites, unregistered service, lingering service suppression, wrong packed modes, mismatched DTB/cpio/root hash and secret-bearing public output. Verification: `python3 tests/test-debian-image.py` and `python3 tests/test-foundation-artifacts.py` RED/GREEN. Produce `E/artifact-fixture-tests.txt`; complete when fixture inspection rejects each mismatch without hardware calls and never substitutes a source cpio for the embedded kernel payload.

## 5. Image/build work and aggregate host gates

- [ ] 5.1 **[Image/build] Assemble the first locked root without touching target media.** Prerequisites: 2.3–2.4, 3.1–3.5, 4.1–4.5; host dependency installation and scratch use explicitly approved. Run the planned `bash debian/build-rootfs.sh --lock debian/inputs.lock.json --output "$W/build-a"`. If a formatter is blocked, hand Cassie the exact regular-file-only command and stop; do not bypass controls. Verify unmounted image with `/sbin/e2fsck -fn "$W/build-a/rootfs.ext4"`, `python3 debian/inspect-rootfs.py --image "$W/build-a/rootfs.ext4" --lock debian/inputs.lock.json --public`, and `bash debian/verify-lifecycle.sh --root "$W/build-a/rootfs"`. Produce `E/build-a.log`, inventory, graph and inspection reports; complete when real commands finish and image/service checks pass, labelled host/image not PSTV.

- [ ] 5.2 **[Image/build] Prove repeat assembly and package-maintenance graph stability.** Prerequisite: 5.1. Assemble independently with the same command and `--output "$W/build-b"`; compare locked package inventory and normalized configuration through a new `debian/compare-builds.py` with `tests/test-build-comparison.py`. Execute the approved upgrade fixture in a disposable host-emulated copy, regenerate and reverify the actual graph/hooks; this is not the native package pass. Verification: `python3 tests/test-build-comparison.py`, then `python3 debian/compare-builds.py "$W/build-a" "$W/build-b"` and the graph gate. Produce `E/rebuild-comparison.json` and `E/upgrade-image-graph.txt`; complete when reproducibility and post-maintenance guard behavior are demonstrated with nondeterminism declared.

- [ ] 5.3 **[Image/build] Exercise real disposable-image handoff and cleanup.** Prerequisites: 3.2, 4.3–4.4, 5.1; explicit privileged-host-test authorization. Create `tests/test-foundation-mounts.sh` with a regular-file-only scratch contract and verify it refuses raw media before running `sudo bash tests/test-foundation-mounts.sh --scratch "$W/mount-gate"`. Use isolated mount namespaces/disposable image files and stub terminal reset; cover outer/inner lifetime, successful and failed moves, busy cleanup, free-loop allocation and guarded terminal refusal. Produce `E/mount-gate.txt`; complete when actual mount observations support ownership/unwinding and no host mount/loop leaks remain. Unavailable privilege is blocked I2, not a fixture pass.

- [ ] 5.4 **[Host implementation / integration] Wire nonprivileged gates into the normal workflow.** Prerequisites: 2.1–4.5 and comparison fixtures in 5.2. Modify `Makefile` and `.github/workflows/build.yml` so a proposed `make test-foundation` runs the new nonprivileged tests and is included in `make test`; retain existing DTB/cart tests. Privileged image gates remain separate and opt-in. Verification: `make test-foundation`, `make test CART_CROSS_CC=arm-linux-gnueabihf-gcc`, and review CI command equivalence. Produce `E/aggregate-host-tests.txt`; complete when the real full host gate finishes, with unavailable cross compiler reported rather than skipped silently. No CI status claim until an actual CI run exists; no push is authorized by this task list.

- [ ] 5.5 **[Image/build] Produce and inspect the rescue/kernel pair from the pinned inputs.** Prerequisites: 3.3, 4.5, 5.1, 5.4. In approved clean build scratch, record exact resolved toolchain/configuration, then use existing `make rootfs LINUX_VITA_DIR="$PWD/linux_vita"`, `make config LINUX_VITA_DIR="$PWD/linux_vita" CROSS_COMPILE="$CROSS_COMPILE"`, `make build LINUX_VITA_DIR="$PWD/linux_vita" CROSS_COMPILE="$CROSS_COMPILE"`, and `make verify-dtb LINUX_VITA_DIR="$PWD/linux_vita" CROSS_COMPILE="$CROSS_COMPILE"`; resolve `$CROSS_COMPILE` from the pinned recipe and verify its executables first. No implicit `.linux-vita-dir` redirect. Use `bash tools/vita-manifest.sh --kernel-dir "$PWD/linux_vita" --outer-dir "$PWD" --model pstv --payload "$W/loader/payload.bin" --kplugin "$W/loader/kplugin.skprx" --out "$W/boot-manifest.json"` with explicitly verified loader inputs and the new root-manifest association. Produce public-safe `E/boot-build.txt` and paired manifest; complete only after extracted embedded init/selector/profile/tools equal the intended artifacts and model DTB checks pass. The helper's existing private-overlay requirement must be handled explicitly by public/private inspection modes, never bypassed with fabricated files.

## 6. Hardware runbook and authorization packet — no device actions yet

- [ ] 6.1 **[Host implementation / gate tooling] Create bounded target collectors and package fixtures.** Prerequisites: 1.2, 2.1, 3.4, 4.4. Create `debian/gates/collect-state.sh`, `debian/gates/package-cycle.sh`, `debian/gates/check-persistence.sh`, `debian/gates/fixtures/thread.c`, and `tests/test-native-gates.py`. Collect boot/PID1/mount/network/package identities without secrets; make mutating package phases explicit, pin the actual upgrade pair, record maintainer-script/service results, and preserve version/hash snapshots. Include native C/pthreads compilation, home/config state and separate removal fixture. Verification: `python3 tests/test-native-gates.py` RED/GREEN with timeout, wrong-root, no-op-upgrade, truncated-output and mismatched-boot-ID fixtures. Produce `E/native-gate-tests.txt`; complete when collectors fail closed and never autonomously deploy, reboot or change networking.

- [ ] 6.2 **[Host documentation] Write the exact operator runbook and evidence template.** Prerequisites: 1.3, 5.2–5.5, 6.1. Create `docs/PSTV-FOUNDATION-TRIAL.md` and `debian/gates/record-template.md`; include staging/readback, external-host override, invalid-root disposable copy, simulated/exercised unconfirmed boot, reset, timing, local-input gate, offline health checkpoints, terminal failure response and VitaOS rollback. Inspect research scripts read-only as methods, never execute them from the research tree. Verification: a second-reader dry walkthrough maps every P0–P4 spec scenario to an exact command or named physical observation, with no blank destructive-command variables and no direct-root hot-unplug. Produce `E/runbook-review.md`; complete when operator actions, ownership and stop conditions are executable and all broader lanes remain explicitly deferred.

- [ ] 6.3 **[Image/build / private enrollment] Freeze the candidate packet and prove host-side backup restore.** Prerequisites: 5.2–5.5, 6.2; approved private enrollment inputs. Make a private copy using 2.4, identify the actual enrolled USB/root filename, preserve the original root/rescue artifacts, inspect both public and private classifications, and record final hashes/fingerprints. Demonstrate a representative backup restore on disposable host media with an integrity/file-metadata comparison; actual target backup is still 7.1. Verification: paired manifest, effective `sshd -t`/`sshd -T`, service graph, offline image checks and complete `sha256sum` results. Produce `E/candidate-packet.md` plus content-free inspection/restore report; complete when every candidate and rollback artifact has exact identity, secret-bearing files stay outside E, and hardware remains marked not run.

## 7. Supervised PSTV validation — hard authorization boundary

Do not begin any task in this section without a new explicit hardware authorization. Cassie selects the actual USB/backup/disposable restore media, assigns one hardware owner and agrees to a window with HDMI, keyboard, controller and offline reader access. A planning or H/I pass is not that approval. Each distinct mutation must be within the approved packet; repair/repartition/format/live-resize remain excluded.

- [ ] 7.1 **[Hardware validation / P0] Inventory, back up and health-gate the actual medium.** Prerequisites: 6.3 and explicit supervised approval. Use the reviewed `docs/PSTV-FOUNDATION-TRIAL.md` preflight: confirm model/firmware/loader/current artifacts, content-based USB identity and media layout; back up existing data and metadata to the selected destination and demonstrate restore on approved disposable media. With consumers stopped and filesystems unmounted, run exact read-only `fsck.exfat -n` and `e2fsck -fn` commands from the packet and measure allocation headroom. Produce `E/P0-preflight.md`, backup/restore/hash and raw health logs. Complete when actual prerequisites pass and original card/eMMC/data are unchanged; dirty/unreadable/ambiguous media blocks all staging, without automatic repair.

- [ ] 7.2 **[Hardware deployment / P1-rescue] Stage the separately named candidate and prove rescue first.** Prerequisites: 7.1 and per-packet artifact approval. Transfer root to a distinct partial path, complete a whole-file destination hash and activate only after it matches; budget the historical long hash time without treating a timeout as success. Preserve and readback-verify old zImage/DTB/loader assets before replacing only the approved candidate boot files. Boot explicit rescue using the external-host override; record local console/rescue identity and no automatic write mounts. Produce `E/P1-rescue.md` plus actual transfer/readback/boot logs. Complete when rescue works independently and original root/workspace/toolkit content is intact; staging alone is not a boot pass.

- [ ] 7.3 **[Hardware validation / P1-recovery] Exercise absent, invalid and unconfirmed-root recovery.** Prerequisite: 7.2. Follow the runbook with safely powered-off USB removal, disposable invalid-root and corrupt/history fixtures, exhausted-attempt state, and an approved disposable normal-root copy with confirmation deliberately withheld. The latter must actually cross handoff and leave an unconfirmed attempt; retain healthy local control for orderly return, never panic PID1 or cut power mid-write. Repeat the bounded unconfirmed path to its specified rescue threshold and restore the enrolled valid candidate afterward. Produce `E/P1-recovery.md` with per-case boot IDs, selected reason, exact fixture identities and external-host reset/override evidence. Complete when each planned case selects rescue/normal exactly as specified; injected-history tests alone do not prove post-handoff recovery.

- [ ] 7.4 **[Hardware validation / P1-normal] Verify normal PID1, mounts and access.** Prerequisite: 7.3 with restored valid candidate. Run the reviewed state collector and manually demonstrate local keyboard login plus authorized SSH, intended `/proc/1/exe`, rw ext4 loop root, enrolled backing identity, ephemeral runtime state and durable matched confirmation. Observe for the packet's bounded 420-second boot window; diagnose HDMI/history/network on timeout rather than assuming kernel failure. Produce `E/P1-normal.md` and raw collector/authentication outcomes without secrets. Complete when actual login/root/PID1 are proven on the exact candidate, not merely a listening port.

- [ ] 7.5 **[Hardware validation / P2] Exercise the native package lifecycle and prepare persistence witnesses.** Prerequisite: 7.4. Run the explicitly approved phases of `debian/gates/package-cycle.sh` natively: authenticated update, install/run, actual version-changing upgrade, separate package removal and `dpkg --audit`. Capture maintainer scripts and any service restart outcomes; regenerate/recheck lifecycle graph and guard after package maintenance. Compile/run `thread.c` with `cc -Wall -Wextra -pthread`, save package versions/database query, config edit, home marker, program hash and SSH fingerprint through the gate tools. Produce `E/P2-native.md`, raw apt/dpkg/program and graph logs. Complete when every transaction truly occurred and witnesses are captured; a no-op upgrade or host-emulated result leaves P2 open.

- [ ] 7.6 **[Hardware validation / P2–P3] Prove two graceful restart transitions and one orderly cold start.** Prerequisite: 7.5, successful guard/graph checks, continued supervised approval. Capture console lifecycle order/mount states and complete one orderly restart to VitaOS; before relaunching Linux, safely quiesce/remove the USB as specified and inspect both filesystems read-only on the approved host. Repeat a second distinct restart and one orderly poweroff/cold start, checking before subsequent Linux writes each time. Use `check-persistence.sh` after each boot to compare package/version/removal state, config/home/program data, SSH identity and boot IDs. Produce `E/P3-cycles.md`, raw phase/console logs, offline checks and persistence comparisons per cycle. Complete only when all declared samples pass with no unexplained storage faults; never call `shutdown-synced` full-unmount proof, and never turn a repair requirement into a pass.

- [ ] 7.7 **[Hardware validation / P4] Re-run protected baseline input/network checks.** Prerequisites: 7.4–7.6 and the required physical keyboard/controller. Follow the runbook for four CPUs, framebuffer/local-console operation, real USB keyboard events, Wi-Fi/authorized SSH, USB-root access and previously validated Bluetooth controller operation. Capture pairing-state persistence without key material, using a further orderly cycle if not captured during 7.6. Produce `E/P4-baseline.md` with device identities, event evidence, relevant fault scan and exact candidate. Complete when all required core connected-device checks pass; the Bluetooth controller and pairing-persistence result is reported separately and neither closes nor by itself blocks the core verdict, while an absent controller is recorded as blocked and never as withdrawn historical Bluetooth HID success. Wired Ethernet, audio and handheld gates remain deferred.

- [ ] 7.8 **[Hardware rollback validation] Demonstrate the independent rollback route.** Prerequisites: 7.2–7.7 for positive milestone closure, or invoke immediately on any failed gate under its approved recovery packet. Follow the runbook through offline override and, when required, VitaOS boot-artifact restoration; verify exact restored readback bytes and boot/access without relying on the failed root. Preserve original data and failed-candidate evidence. Produce `E/rollback.md` with actual return-to-known-good outcome. Complete when restoration is demonstrated, not merely scripted or named; final deployed state is recorded explicitly and no candidate is silently re-promoted.

## 8. Evidence reconciliation and review closure

- [ ] 8.1 **[Evidence / host-verifiable] Assemble the acceptance matrix without promoting gaps.** Prerequisites: completed outputs from H/I/P tasks, or a clearly blocked disposition for unfinished hardware work. Update `docs/evidence/pstv-foundation.md` and create `E/RESULTS.md` mapping every requirement/scenario to its exact record, venue and status; retain negative/inconclusive outcomes and what each pass does not prove. Verification: independent citation/hash/exit-status review, content-free secret scan and programmatic accounting of required scenarios. Complete the evidence task when all claims have honest dispositions; the foundation milestone remains incomplete unless every required positive gate passed.

- [ ] 8.2 **[Documentation / host implementation] Reconcile public status with the bounded result.** Prerequisite: 8.1. Update `README.md`, `docs/PROJECT-STATUS.md`, `docs/HARDWARE-ROADMAP.md`, `docs/architecture/linux-system.md`, `docs/plans/2026-09-13-linux-system-roadmap.md` and `debian/README.md` with dated supersession, implemented recipe commands and exact accepted/not-run scope. Preserve old evidence, sysvinit/8 GiB chronology, original Bluetooth success and deferred native partition/Alpine/handheld lanes. Verification: second-reader comparison against `E/RESULTS.md`, no consumer-distribution/power-loss/all-Vita claim, and all referenced commands/paths exist. Produce `E/docs-review.md`; complete when prose matches actual results, not the number of checked tasks.

- [ ] 8.3 **[Review / final verification] Verify the complete change and stop at the authorized boundary.** Prerequisites: 8.1–8.2 and every required positive gate for a successful foundation outcome. Run `make test-foundation`, `make test CART_CROSS_CC=arm-linux-gnueabihf-gcc`, `openspec validate pstv-general-purpose-linux-foundation --type change --strict --no-interactive`, `openspec status --change pstv-general-purpose-linux-foundation --json`, and scoped Git diff/status checks. Obtain independent spec-compliance and safety review; produce `E/final-review.md` and a resume record naming actual device state, artifacts and open gates. Complete only when all acceptance is accounted for. No automatic commit, push, PR, archive or further deployment follows.

Dependency summary: 1 -> 2; 2.1 -> 3; 1.3 + 3 -> 4; 2–4 -> 5; 5 + 6 -> **human P0 boundary** -> 7 -> 8. Host tasks can be split into test-first micro-steps; device tasks are strictly serialized. Design gate IDs H1/I1/I2/P0–P4 are acceptance categories, not claims of execution. V1 and all non-goals are intentionally outside this task checklist.
